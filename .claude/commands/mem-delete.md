---
description: Remove a project memory (manual memory).
argument-hint: "<which memory>"
---

Delete a memory: $ARGUMENTS

1. Read `MEMORY.md` and locate the target memory file.
2. Delete that memory file and remove its pointer line from `MEMORY.md`.
3. Confirm exactly what was removed. If no memory matches, say so and list candidates — do not delete anything when unsure.
