---
description: Show a summary of all stored project memories (manual memory).
---

Show all memories in the project memory store.

Read `MEMORY.md` and the memory files in the `memory/` directory, then present a view-all summary as a table with one row per memory: **name · type · description**. If the store is empty, say so plainly. Do not modify anything.
