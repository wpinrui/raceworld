---
description: Edit an existing project memory (manual memory).
argument-hint: "<which memory> — <what to change>"
---

Update an existing memory: $ARGUMENTS

1. Read `MEMORY.md` and locate the target memory file (match on slug, title, or description).
2. Edit that file in place, preserving the frontmatter shape (`name`, `description`, `metadata.type`). Update `description` if the summary changed.
3. Keep the `MEMORY.md` pointer line in sync if its title or hook changed.
4. Confirm what you changed. If no memory matches, say so and list the closest candidates instead of guessing.
